<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>github.jiangbyte.io</groupId>
        <artifactId>app</artifactId>
        <version>${revision}</version>
    </parent>
    <artifactId>admin</artifactId>
    <packaging>jar</packaging>

    <dependencies>
        <dependency>
            <groupId>io.swagger.core.v3</groupId>
            <artifactId>swagger-annotations-jakarta</artifactId>
        </dependency>
        <dependency>
            <groupId>org.postgresql</groupId>
            <artifactId>postgresql</artifactId>
        </dependency>
        <dependency>
            <groupId>com.mysql</groupId>
            <artifactId>mysql-connector-j</artifactId>
        </dependency>
        <!-- Flyway 依赖保留（库表由 scripts/db.sql 重建；如需迁移可在 resources/db/migration 重新放置 V{n}__*.sql 并启用） -->
        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-core</artifactId>
        </dependency>
        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-database-postgresql</artifactId>
        </dependency>

        <!-- business modules: explicit deps, no bundle -->
        <dependency>
            <groupId>github.jiangbyte.io</groupId>
            <artifactId>auth</artifactId>
        </dependency>
        <dependency>
            <groupId>github.jiangbyte.io</groupId>
            <artifactId>iam</artifactId>
        </dependency>
        <dependency>
            <groupId>github.jiangbyte.io</groupId>
            <artifactId>sys</artifactId>
        </dependency>
        <dependency>
            <groupId>github.jiangbyte.io</groupId>
            <artifactId>profile</artifactId>
        </dependency>
        <dependency>
            <groupId>github.jiangbyte.io</groupId>
            <artifactId>workspace</artifactId>
        </dependency>
        <!-- sample module/biz: see profile with-biz (activeByDefault); prod: -P'!with-biz' -->

        <dependency>
            <groupId>github.jiangbyte.io</groupId>
            <artifactId>common-oss</artifactId>
        </dependency>
        <dependency>
            <groupId>github.jiangbyte.io</groupId>
            <artifactId>common-doc</artifactId>
        </dependency>
        <dependency>
            <groupId>github.jiangbyte.io</groupId>
            <artifactId>common-log</artifactId>
        </dependency>
        <dependency>
            <groupId>github.jiangbyte.io</groupId>
            <artifactId>common-job</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>
        <dependency>
            <groupId>io.micrometer</groupId>
            <artifactId>micrometer-registry-prometheus</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-opentelemetry</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <profiles>
        <profile>
            <id>with-biz</id>
            <activation>
                <activeByDefault>true</activeByDefault>
            </activation>
            <dependencies>
                <dependency>
                    <groupId>github.jiangbyte.io</groupId>
                    <artifactId>biz</artifactId>
                </dependency>
            </dependencies>
        </profile>
    </profiles>

    <build>
        <finalName>${project.artifactId}</finalName>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <executions>
                    <execution>
                        <goals>
                            <goal>repackage</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
