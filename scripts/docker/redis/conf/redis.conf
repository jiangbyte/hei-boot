# 与 scripts/docker/.env 中 REDIS_PASSWORD 一致（由 compose 启动时注入）
# requirepass 勿写在此文件，避免密码进入 Git

dir /redis/data

save 900 1
save 300 10
save 60 10000
rdbcompression yes
dbfilename dump.rdb

appendonly yes
appendfilename "appendonly.aof"
appendfsync everysec
